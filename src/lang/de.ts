import { ResourceLanguage } from "i18next";

const translations: ResourceLanguage = {
  navbar: {
    home: "Startseite",
    about: "Über mich",
    projects: "Projekte",
  },
};

export default translations;
